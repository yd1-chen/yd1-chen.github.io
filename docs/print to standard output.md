- [Go](Go.md)
```Go
package main

import "fmt"

func main() {
    // automatically add spaces to separate between the parameters, and automatically append a line break at the end
    fmt.Println("an arbitrary number and type of parameters, such as strings, numbers, booleans, and structures")
}
```